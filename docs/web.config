<?xml version="1.0" encoding="UTF-8"?>
<configuration>
  <system.webServer>
    <staticContent>
      <!-- Allow IIS to serve raw Markdown files (used by "View as Markdown" feature) -->
      <mimeMap fileExtension=".md" mimeType="text/plain" />
    </staticContent>
  </system.webServer>
</configuration>
